# Sprint plan — Events as composition substrate — durable delivery on a verified core, with the human surface in the loop

*Generated 2026-07-05 by the `sprint-plan` workflow (v2: split Review→Revise→Refine) — 19 agents. Discovery on Sonnet; cross-cutting analysis and the pragmatic revise on Fable@high; synthesis/plan/lens-review/refine on Opus. Draft → 3-lens review → pragmatic Fable revise → mechanical Opus refine.*

## Theme

The events engine is built through Step 3 but its payoff is unproven, and review verified the plan's original foundation story was partly stale: ClaimAtomic is already fixed at HEAD, while the real log-integrity gap is fireOne's mark-then-append crash window — which would silently defeat the outbox on the exact path it demos. This sprint therefore verifies-then-hardens the actual gaps first, ships the attachments evidence loop end-to-end including the /ui surface humans actually look at, dogfoods the real board (web UI included) to produce a pre-registered go/no-go signal, and only then builds outbox + webhooks — with the RFC resolving the Subscriber name collision, ephemeral-condition deliverability, and dispatch topology before any code. Value curve stated honestly: Phases 1 and 4 are insurance (their demos show concretely averted loss), Phases 2/3/5 are visible value for humans, operators, and integrators respectively. The hard line holds: conditions only ever emit — the core never acts.

**North star:** Convert the events investment from a potential in-core engine into the composition substrate the philosophy demands: land Step 4 (durable outbox/tailer) and then the webhook delivery kind, so the completed condition-events machinery becomes something external processes consume — not something the core acts on. This is the single most vision-advancing direction because it is the direct test of 'small core, big composition' and 'hooks, not engines': it makes the largest existing investment pay off for the USER (reliable, replayable reactive coordination instead of drop-on-crash SSE) while keeping all reaction outside the core. Do it in parallel with the low-risk, high-leverage attachments wiring (a kept-small core capability that is fully built but dark), and hold the line that conditions only ever emit — they never mutate.

## Phases

### Phase 1 — Verify the ledger, then harden the real event-log gaps

**Goal:** Reconcile the debt ledger against live code FIRST (it already misled this plan once — the original ClaimAtomic step targeted code fixed at HEAD), then close the gaps that actually remain: the fireOne mark-then-append atomicity window that no downstream outbox can recover, the swallowed SSE-replay/idempotency errors, and a graceful-drain goroutine-shutdown template the tailer and webhook worker will reuse. Beneficiary: operator/maintainer — insurance, framed as averted loss.

| Do | Outcome (acceptance) | Likely files |
|---|---|---|
| Ledger-and-docs reconciliation as the FIRST step: verify every DEBT entry this sprint touches against HEAD (DEBT-01/02/30/17 are already fixed in ClaimAtomic — reduce to a pinning regression test, no re-fix; confirm what remains of DEBT-03 AddLink/AddComment and DEBT-02 scanCard), correct stale path references (the ledger still cites internal/httpapi/httpapi.go, which has been split into api.go/sse.go/middleware.go), correct the stale DEBT-57 artifacts.Manager entry, refresh EVENTS.md/inventory counts, archive completed planning artifacts to docs/archive, resolve the dangling docs/plans/sprint-2026-07-05.md deletion. | Reconciliation produces a written per-entry disposition (fixed-at-HEAD vs still-remaining, with the correct current file/line) for every DEBT this sprint touches, so downstream steps cite verified live code rather than ledger prose; ClaimAtomic's existing rollback/version behavior is pinned by a regression test; git status clean. | debt-ledger.md, inventory.md, docs/events/EVENTS.md, internal/sqlite/claimatomic_test.go (pin test), docs/archive/ |
| Close the fireOne atomicity gap for persist:true conditions: MarkConditionFired and the durable event append currently commit in separate transactions (monitor.go:340 marks, append follows), so a crash between them permanently loses the event AND suppresses re-fire — unrecoverable by any later cursor replay. Make mark+append one transaction (or append-then-mark with idempotent replay). Fix DEBT-06 (swallowed SSE replay error) and DEBT-07 (swallowed PutIdempotency) here too, so they don't ride the gated XL phase. | A forced crash between verify and dispatch can no longer strand a fired-but-never-appended condition; a fault-injection test proves the marks row and log row commit together; SSE replay and idempotency errors surface instead of vanishing. | internal/core/monitor.go fireOne, internal/core/events.go, internal/sqlite/sqlite.go (condition_marks tx), internal/httpapi/sse.go (DEBT-06 replay), internal/httpapi/middleware.go (DEBT-07 PutIdempotency) |
| Reframe the supervisor-lifecycle work around what's actually missing: serve.go already cancels the supervisor ctx on ListenAndServe exit, but cancellation is fire-and-forget — no WaitGroup/drain, so shutdown doesn't await goroutine exit or in-flight hook spawns. Add a graceful-drain convention (ctx + WaitGroup, bounded drain timeout) and document it as the template the outbox tailer and webhook worker must reuse. | Shutdown provably awaits worker exit (test asserts no goroutine leak and in-flight hook completion or bounded abandonment); the pattern is documented where Phase 4/5 will find it. | internal/hooks/hooks.go (Supervisor), cmd/cards/serve.go, internal/httpapi/server.go |

**Demo:** Show the averted user-facing loss, not just a green suite: trigger a persist:true condition, kill the process in the old mark-then-append window, restart — under the old code the escalation is gone forever with no trace and never re-fires; under the new code it either never marked or is in the log. 'Here is the alert you would silently never have received.'

**Exit criteria:**
- Ledger/inventory/EVENTS.md verified against HEAD with a per-entry fixed-vs-remaining disposition; already-fixed entries closed with pin tests instead of re-work; working tree clean
- fireOne mark+append is atomic for persist:true conditions, with a crash-window fault-injection test
- DEBT-06/DEBT-07 fixed independently of the outbox phases
- Graceful-drain shutdown convention landed on the hook Supervisor and documented as the worker template; suite green

### Phase 2 — Wire attachments end-to-end, including the human's browser view

**Goal:** Convert the largest built-but-dark capability into shipped value for ALL consumers — explicitly including the human at /ui, who today would upload a screenshot and be unable to see it anywhere (card_detail.html renders no artifacts, comments, or links). The store only holds bytes and emits a signal, never reacts. Beneficiary: human via UI + agent via MCP + scripts via CLI/HTTP.
  
**Depends on:** Phase 1 — Verify the ledger, then harden the real event-log gaps

| Do | Outcome (acceptance) | Likely files |
|---|---|---|
| Add a shared workspace-resource helper that builds artifacts.Manager once from the resolved workspace dir and thread it through serve.go, directcli.go, httpapi, and MCP so the surfaces can never diverge on where bytes land. | One helper owns 'workspace dir -> artifacts root'; a test asserts HTTP, CLI-direct, and MCP resolve the identical on-disk root. | cmd/cards/workspace.go (new helper), cmd/cards/serve.go, cmd/cards/directcli.go, internal/httpapi/server.go, internal/mcp/mcp.go |
| Before wiring surfaces, pin the semantics in a test: Service.AddArtifact's overwrite/version-conflict behavior against service.go's optimistic-concurrency checks, and the artifact_added payload shape (field id, Meta, overwrite flag) via a proper constructor for the already-declared EventArtifactAdded type (types.go:291, which has no constructor/Diff today). Then implement AddArtifact(ctx, id, field, io.Reader): Manager.Put, write-time ArtifactPolicy confinement check, commit, emit. | Conflict and overwrite behavior is a tested contract before any surface exists, so CLI/HTTP/MCP cannot diverge on it; artifact_added reaches the bus (hook Supervisor gets it free), asserted by test. | internal/core/service.go, internal/core/events.go (constructor), internal/core/validate.go, internal/core/types.go, internal/core/service_test.go |
| Add the HTTP upload endpoint + serve route, the `cards attach` CLI verb (inherits HTTP semantics via the recorder direct path), and the MCP tool with documented base64-in/out encoding and size limit (stdio JSON-RPC has no multipart — deliberate asymmetry, documented). | `cards attach <card> <file>` uploads; GET serves bytes back identical; MCP round-trips a small file as base64; artifact_added streams on `cards feed`. | internal/httpapi/api.go + server.go, cmd/cards/commands (new verb), internal/mcp/mcp.go buildTools() |
| Close the human loop: render attachments on card_detail.html (inline thumbnail for images; download link + MIME/size for others) and — since the template is open and the evidence-on-a-card view is otherwise empty — render existing comments and links in the same thin evidence pane. Add artifact_added to boardSSETypes() so an attached artifact appears on the live board without reload. | A human attaches a screenshot and SEES it on the card in the browser; the live board reflects artifact_added via SSE; comments/links stop being invisible on the one human surface. | internal/httpapi/templates/card_detail.html, internal/httpapi/ui.go boardSSETypes(), template funcs |
| Route-level adversarial path-traversal tests (chi param decoding, trailing slashes, encoded '..'/%2e%2e) against the wired serve route, and add the artifacts paths to the hand-authored OpenAPI table. | Encoded-traversal requests against the live route return 404/400, never escape the artifacts root; OpenAPI lists the routes. | internal/httpapi/httpapi_test.go, internal/openapi/openapi.go paths() |

**Demo:** Attach a screenshot via `cards attach`, open the card at /ui/cards/{id} and see the thumbnail render in the browser, watch the live board update via SSE without reload, retrieve the same file via the MCP tool as an agent would, then fire a batch of encoded-traversal requests and watch them all get rejected at the route.

**Exit criteria:**
- Upload + serve + CLI + MCP + /ui render all work against one shared artifacts root, with artifact_added emitted and visible on the live board
- Overwrite/version-conflict semantics and artifact_added payload pinned by tests before surface wiring
- Route-level traversal tests pass through real routing/middleware
- ROADMAP §4 / card dafd0873 moves to built; MCP base64 asymmetry + size limit documented; suite green

### Phase 3 — Dogfood the backlog on the live board (web UI included) and produce the outbox go/no-go

**Goal:** Close the missed validation loop on the surfaces where coordination actually happens — including /ui/boards/{id}, not just CLI/MCP — and emit the sprint's pivotal artifact: a pre-registered go/no-go decision on the XL outbox. JSONL export scope is explicit: authoritative for definitions + current card state ONLY; the event journal, condition_marks, and all future cursor/dead-letter/subscription state remain SQLite-owned durable ground truth that a rebuild must never touch. Beneficiary: the maintaining team as real users.
  
**Depends on:** Phase 2 — Wire attachments end-to-end, including the human's browser view

| Do | Outcome (acceptance) | Likely files |
|---|---|---|
| Promote the JSONL export to canonical for card definitions + current card state, seeded from examples/demo-workspace and reconciled with ROADMAP card titles — while explicitly declaring the events table, condition_marks, and durable-delivery tables authoritative and non-rebuildable (import reconstructs card state; it never truncates or reconstitutes history/delivery state). Strictly one-way; bidirectional PATCH-mirror deferred. | A fresh clone rebuilds the real backlog via `cards import`; an import against an existing workspace provably leaves event history and delivery state intact (test asserts event count unchanged across rebuild). | cmd/cards/export.go, import.go, portable.go, examples/demo-workspace/definitions/*, docs/events/EVENTS.md (durable-state ownership note), .gitignore |
| Add a CI/pre-commit check that fails if any docs/*.md card short-id fails to resolve against the committed export. | A doc citing a nonexistent short-id fails CI; all 23 currently-cited ids resolve or are fixed. | scripts/ (new check), CI workflow, docs/ROADMAP.md, docs/NOTES.md |
| Run the daily loop for the sprint through ALL surfaces including the web UI: claim and transition via /ui board and CLI, MCP take_next/append_entry, attach a real build log (Phase 2) and see it on the card page. Keep a friction log covering the web board/card-detail experience, not just terminal ergonomics. Conclude with the pre-registered go/no-go artifact for the outbox: the criterion is written down BEFORE dogfooding starts (e.g. 'events of classes the team relies on — review-notify transitions, condition escalations — observed dropped across an induced restart/crash during real use'), and the decision rule is structural: no-go means Phase 4 stops after its RFC step and Phase 5 is cut from the sprint. | A friction log spanning web + CLI + MCP; review-notify fired on genuine transitions; a written go/no-go verdict against the pre-registered criterion, with board screenshots as evidence alongside the committed JSONL. | examples/demo-workspace (live board), internal/httpapi/ui (exercised), internal/mcp (exercised), internal/hooks (review-notify in anger), docs/design/OUTBOX-GONOGO.md (criterion + verdict) |

**Demo:** Live-drive the maintainers' backlog on the actual /ui board: MCP take_next claims the next real card, the column updates live in the browser, attach a build log and see it render on card detail, transition and watch review-notify fire — then break a doc short-id and show CI go red, fix it, green. Close by reading out the go/no-go verdict.

**Exit criteria:**
- Committed JSONL reconstructs card state on a fresh clone; a rebuild against an existing DB never drops event history or delivery state (tested)
- Doc short-id CI check enforced and green
- Team backlog coordinated on the board through web UI + CLI + MCP, with a friction log covering the human surface
- OUTBOX-GONOGO.md exists with a pre-registered criterion and a recorded verdict; no new endpoints/fields/event types added

### Phase 4 — Subscriber RFC (naming, deliverability, topology), then the gated durable outbox

**Goal:** Design once, on paper, the three things that are design failures if drifted into: (1) the name collision — core.Subscriber (bus.go:51) is an ephemeral in-process listener with no cursor, a different species from the durable entity; (2) ephemeral-condition deliverability — condition events default to the Signal path and never enter the log, so a durable consumer's filter is silently a no-op for half the catalog; (3) dispatch topology — Emit already publishes synchronously to bus/observers, so a tailer that 'republishes' double-delivers. Then, ONLY on a go verdict from Phase 3, build the cursor+dead-letter outbox on the Phase-1-hardened log and drain convention. Beneficiary: operator/integrator — insurance, demoed as averted loss.
  
**Depends on:** Phase 1 — Verify the ledger, then harden the real event-log gaps, Phase 3 — Dogfood the backlog on the live board (web UI included) and produce the outbox go/no-go

| Do | Outcome (acceptance) | Likely files |
|---|---|---|
| Write docs/design/SUBSCRIBERS.md resolving, before any code: naming (rename the durable entity — e.g. Consumer — or the bus struct, with an explicit mapping from ephemeral core.Subscriber to the durable model); per-type deliverability (registering a durable consumer for an ephemeral condition type either auto-escalates it to persist:true — specifying how that squares with PersistConditions' startup-only invariant and MonitorScheduler arming off IsPersisted — or is rejected at registration, and EVENTS.md gains a versioned per-type durable/ephemeral column); dispatch topology (tailer feeds only new durable consumers, bus/observers stay synchronous — or replaces sync dispatch, re-specifying the observer/hook synchrony contract); where durable interest is computed for MonitorScheduler so liveness has one owner; the hooks-migration and static-vs-runtime forks; auth/SSRF for /v1/subscribers; migration ordering relative to migrateEventsScope's table rebuild using the existing idempotent-DDL pattern; and deterministic fake-clock injection (internal/core/clocktest) as a requirement for retry/backoff tests. | One agreed model answering naming, deliverability, topology, liveness ownership, hooks fork, auth/SSRF, migrations, and testability. This step happens regardless of the go/no-go; the remaining steps are gated on 'go'. | docs/design/SUBSCRIBERS.md (new), docs/events/EVENTS.md §8/§9/§12 |
| [Gated: go verdict] Implement the durable per-consumer cursor + background tailer reading the events table in id order, feeding durable consumers only per the RFC's topology (no double-delivery to existing bus subscribers), plus dead-letter records — built on the Phase-1 drain convention, with fake-clock-driven backoff tests. | A consumer with a stored cursor disconnects, the server restarts, and on reconnect it receives every missed event in order exactly once-or-more per the RFC; existing bus/SSE subscribers receive each event exactly as often as before (regression-tested); tailer drains cleanly on shutdown. | internal/core/events.go, bus.go, internal/sqlite/sqlite.go (cursor + dead-letter tables), internal/core tailer |
| [Gated: go verdict] Unify consumer liveness per the RFC so MonitorScheduler treats a disconnected-but-behind durable cursor as still interested, and add regression coverage for what the async path could visibly break: temporal_test.go exact-timing assertions AND an integration check that the live /ui board's EventSource-driven re-render still receives mutation events correctly ordered under the tailer. | Temporal conditions stay armed for behind-but-durable consumers; temporal and SSE timing tests pass; the human live board demonstrably still updates in order — no stale columns or missed re-renders. | internal/core/monitor.go, bus.go, service.go wiring, internal/httpapi tests (EventSource/board SSE integration) |

**Demo:** Register a durable consumer, generate a breach + card_idle, kill -9 mid-dispatch, restart: the consumer replays from its cursor and receives everything it would previously have lost — including a duplicate-delivery segment showing consumers deduping by event id, so at-least-once is demonstrated honestly. Side-by-side, the /ui board keeps re-rendering live and in order.

**Exit criteria:**
- SUBSCRIBERS.md agreed, resolving naming collision, per-type deliverability, dispatch topology, liveness ownership, hooks fork, auth/SSRF, and migration/testability requirements
- If go: at-least-once delivery across restart via stored cursors with dead-letter and clean drain; no double-delivery to pre-existing bus consumers; if no-go: RFC lands, Steps 2-3 and Phase 5 are cut and re-planned
- Duplicate-delivery (dedupe-by-event-id across mid-dispatch kill) covered by a regression test, not a doc footnote
- temporal/SSE timing tests and the /ui live-board integration check pass; EVENTS.md records commit-vs-dispatch decoupling as a versioned contract

### Phase 5 — Webhook delivery kind and /v1/subscribers with enforced egress allowlist

**Goal:** The composition payoff: conditions delivered outward to independent processes via durable, retryable webhooks, with reaction entirely outside the core. Runtime registration of arbitrary outbound URLs is the first runtime-mutable extension-like entity in a file-declared system, so the SSRF control is an enforced default-deny egress allowlist shipped WITH the surface — a documented assumption is not a control. If the allowlist can't land, webhook subscribers stay file-declared this sprint. Beneficiary: external integrator + operator. Cut entirely on a Phase-3 no-go.
  
**Depends on:** Phase 4 — Subscriber RFC (naming, deliverability, topology), then the gated durable outbox

| Do | Outcome (acceptance) | Likely files |
|---|---|---|
| Implement the webhook delivery kind as a tailer consumer per the RFC: at-least-once with retry/backoff (fake-clock tested), HMAC-signed payloads, per-subscriber dead-letter — reusing the Phase-1 drain and Phase-4 cursor conventions. Deliverability per the RFC: subscribing to an ephemeral condition type auto-escalates or is rejected, never silently no-ops. | status_timeout fires an HMAC-signed POST to a local endpoint; a flaky endpoint is retried then dead-lettered; delivery survives restart via the cursor. | internal/config/extensions.go (Extension/HookFilter), internal/core (tailer consumer), internal/sqlite (subscriptions table) |
| Add /v1/subscribers (create/list/status/delete) reusing withActor/idempotent middleware, exposing delivery attempts, retry counts, last error, and dead-letter state — with an ENFORCED default-deny outbound host/CIDR allowlist (config-declared) validated at registration and at dispatch. JSON-only operator surface this sprint: the /ui health view is explicitly recorded as an accepted gap with a follow-up card, since the breaches-page pattern makes it cheap later but Phase 5 is already L-sized. | GET /v1/subscribers shows cursor and delivery health; registering a URL outside the allowlist is rejected with a clear error; a dispatch-time allowlist re-check blocks DNS-swap tricks; the no-UI gap is a tracked card, not an implicit hole. | internal/httpapi/server.go, api.go, middleware.go, internal/config (egress allowlist), internal/sqlite (delivery-attempt queries) |
| Complete EVENTS.md §9's delivery taxonomy (at-least-once push webhooks vs at-most-once shell hooks vs pull-based idempotent SSE/feed) and reconcile §8.6 escalation restart duplication with webhook retries: integrators must dedupe by event id, with the compounding case documented and pointed at the Phase-4 dedupe regression test. | A versioned contract table an outside integrator can build against, including per-type deliverability and duplicate expectations. | docs/events/EVENTS.md §8/§9, docs/events/INTEGRATION.md |

**Demo:** Wire a webhook subscriber to a throwaway chat endpoint, let a card go idle past threshold, and watch the condition arrive HMAC-signed in the external tool with zero reaction logic in the core; kill the endpoint, show retries then dead-lettering in /v1/subscribers, revive it, watch delivery resume from the cursor; then try to register an off-allowlist URL and watch it refused.

**Exit criteria:**
- Webhooks deliver at-least-once with retry/backoff, HMAC, dead-letter, surviving restart
- /v1/subscribers ships with an enforced default-deny egress allowlist (registration + dispatch time), or runtime registration is withheld and subscribers stay file-declared
- EVENTS.md §9 taxonomy complete, per-type deliverability documented, escalation-plus-retry duplication reconciled
- Core still only emits — no condition triggers an in-core mutation; operator-UI gap tracked as a card; suite green

## Risks
- Capacity: even with Phase 1 slimmed by dropping already-fixed work, Phases 4-5 remain XL+L; the pre-registered go/no-go gate and the 'RFC lands regardless' floor are the pressure valve — durable-delivery code must not be rushed at sprint end, and cutting Phase 5 is the planned response, not a failure
- The fireOne atomicity fix touches the hottest reactive path; append-then-mark vs single-tx choice affects re-fire semantics under crash and must be settled in the Phase 1 step, not discovered in review
- Dispatch topology is the sharpest RFC edge: 'tailer feeds only new durable consumers' preserves the observer synchrony contract but permanently forks two delivery paths; 'tailer replaces sync dispatch' unifies but changes hook timing and SSE latency that tests and the live /ui board assert on — either way the /ui EventSource integration check is the canary
- Ephemeral-condition deliverability interacts with the startup-only PersistConditions invariant; auto-escalation at runtime means mutating emitter config and re-arming MonitorScheduler — if that proves gnarly, reject-at-registration is the cheap safe fallback and the RFC should say so
- Dogfooding can be gamed by committing one snapshot; the CI doc-id check plus the requirement that the friction log cover the web surface are the acceptance gates
- The egress allowlist is dispatch-time as well as registration-time or it is theater (DNS rebinding); if enforcing that slips, the fallback is file-declared webhook subscribers, already specified in Phase 5
- Engine-creep watch continues: reviewers should reject any PR where a condition performs an in-core write

## Out of scope
- Authentication/identity and permissions as core features — auth stays a host/proxy/extension concern; this sprint ships the enforced egress allowlist and the RFC's auth/SSRF note, not an in-core authz gate
- Any in-core reaction to conditions — conditions only ever emit; the moment the core acts on one it becomes the workflow-automation platform it disclaims
- Events Step 4+ beyond outbox/webhooks — no projection/job system, no cross-process broker ambitions
- Bidirectional git-backed mirror / version-gated PATCH-import (NOTES D13) — Phase 3 is strictly one-way export-as-source-of-truth
- Exporting/round-tripping the event journal in JSONL — the reviewers' alternative to the derived-DB contradiction; declined in favor of explicit state-ownership scoping because history/delivery state has one authoritative home (SQLite) and a portable event-log format is a project of its own
- A /ui operator view for subscriber/delivery/dead-letter health — accepted as JSON-only this sprint, tracked as a follow-up card (the reviewer offered this option; Phase 5 is already L-sized)
- Extending the Phase-3 CI check to verify DEBT ledger line:number references against code — the doc short-id check plus the Phase-1 verified reconciliation captures most of the value; full ledger-drift automation is deferred (reviewer suggestion, declined as low value-per-effort once the ledger is re-verified)
- A formal sprint split at the Phase 3/4 boundary — the reliability reviewer's two-sprint proposal is adopted in substance (structural gate: no-go cuts Phase 4 Steps 2-3 and all of Phase 5; the RFC lands regardless and can be reviewed cold) but kept as one plan document, since the gate artifact gives the same protection without re-planning ceremony
- Migrating the hook Supervisor onto durable cursors unless the RFC explicitly decides to
- Alternative storage backends (Postgres) and a backend-neutral filter AST
- GH-PAGES-TODO docs site + Obsidian/Jira/GitHub ETL — if ever built, an external webhook subscriber, never a core feature

---

## Appendix A — cross-cutting analysis (Fable, the hardest step)

**Foundations to tackle first**
- Event-log integrity before any durable-delivery work: the ClaimAtomic cluster (DEBT-01/02/30/17 in internal/sqlite/sqlite.go:445-456) silently drops event-insert/FTS errors and emits hand-rolled Version:0 events. The outbox tailer, webhooks, and SSE replay all read this log as ground truth; a webhook that never fires because an insert was swallowed is undebuggable. Small, well-scoped, already sequenced as fix #1 in the ledger's own plan — do it first.
- A single Subscriber/Consumer abstraction designed once (short RFC), not three times: the outbox card needs per-consumer durable cursors, the webhook card needs subscriptions (id, URL, filter, HMAC secret, cursor, dead-letter state), and MonitorScheduler's HasSubscriberFor liveness check needs to understand 'disconnected but not caught up'. These are the same concept. Designing it before either build prevents two divergent liveness/interest mechanisms (bus.go SetOnSubscriptionChange vs. tailer cursors) and answers the hooks-Supervisor migration question deliberately.
- Goroutine lifecycle convention (DEBT-29): the hook supervisor already outlives ListenAndServe failure because it isn't tied to server shutdown. The outbox tailer and webhook delivery worker are two more long-lived goroutines about to be added. Establish one pattern (shared context.Context / RegisterOnShutdown) now so all three background workers share it instead of each reinventing shutdown.
- Shared workspace-resource construction helper: attachments need artifacts.Manager constructed from the resolved workspace dir at three call sites (cmd/cards/serve.go for HTTP+MCP, cmd/cards/directcli.go). One helper owning 'workspace dir -> on-disk resource roots' avoids silent divergence between CLI-direct and served-HTTP, and sets the precedent ARCHITECTURE.md:328's planned release-artifacts will follow.
- Dogfood-with-enforcement as a process foundation: commit the JSONL export as canonical (SQLite demoted to derived artifact) AND add the CI/pre-commit check that doc-cited card short-ids resolve against the committed export. Without the check, the step just reproduces the 21/23-dangling-refs rot one level up. Cheapest candidate (S), and its real usage data is the input for deciding whether the XL outbox is needed at all.
- OpenAPI path-table decision: internal/openapi/openapi.go's hand-authored paths() is already missing links/comments; artifacts and /v1/subscribers each add another manual line. Decide once — generate from the chi router, or add a CI drift check — before two more surfaces widen the gap.
- Cross-surface byte-transport design note: MCP (JSON-RPC/stdio) can only carry base64 while HTTP gets multipart/raw bodies. Settle the encoding, size limits, and 'this asymmetry is intentional' documentation once for attachments, because it will recur for any future binary surface (export downloads, release artifacts).

**Tech debt to clear**
- DEBT-01 + DEBT-02 + DEBT-30 + DEBT-17 (ClaimAtomic hardening, one file/one pass per the ledger's own fix plan) — direct prerequisite for outbox/webhooks and for trusting artifact_added in the log.
- DEBT-29 (supervisor lifecycle not tied to shutdown) — becomes the template bug for the outbox tailer and webhook worker if not fixed before they're written.
- DEBT-57 correction: the ledger still describes artifacts.Manager as 'an empty struct' with nothing implemented; the actual file has full content-addressing, MIME sniffing, and symlink confinement. This stale entry (plus inventory.md's matching description) actively misleads anyone scoping the attachments card — correct it as the first act of that work.
- DEBT-42/43/44 (stale event-catalog counts and 'events_test.go does not exist' claims) — one editing pass; matters because the attachments card adds artifact_added emission and the counts must not drift further.
- DEBT-45/46 (hook payload doc drift: board_ids field that doesn't exist; service-kind autostart contradiction) — needs the product call before webhook becomes a third Extension.Kind reusing the same filter model; resolving the hook contract precisely is a prerequisite for specifying the webhook one.
- Repo hygiene: archive debt-ledger.md / action-plan.md / reorg-plan.md to docs/archive alongside the already-moved review files; commit or restore the pending deletion of docs/plans/sprint-2026-07-05.md so the newest plan's status is unambiguous.
- DEBT-06 (SSE replay error swallowed) rises in priority the moment durable-cursor consumers exist — fix as part of, not after, the outbox work.

**Domain-model evolution to consider**
- Subscriber as a first-class durable entity (id, filter, cursor, delivery state, dead-letter record) — the one genuinely new domain concept underlying outbox, webhooks, hooks-with-replay, and monitor interest. Currently punted three separate times; should be designed once as the unifying model, with SSE Last-Event-ID reframed as a client-held cursor of the same species.
- Static vs. runtime-mutable extensions: /v1/subscribers would be the first API-created extension-like entity in a system whose extension model is entirely file-declared (definitions/extensions.json). Breaking that invariant is a deliberate architectural fork — and because an unauthenticated endpoint registering outbound URLs is an SSRF surface, it drags the ROADMAP §1 auth non-goal forward. Decide the fork (and write the auth design note) before building the surface.
- Explicit delivery-semantics taxonomy per surface (EVENTS.md §9): at-most-once shell hooks, pull-based idempotent SSE/feed, at-least-once push webhooks — plus how escalated-condition restart duplication (§8 point 6) compounds with webhook retries. Make the contract table complete before a fourth surface ships.
- Artifact mutation as a first-class service operation: a dedicated Service.AddArtifact method with write-time ArtifactPolicy validation and a proper EventArtifactAdded constructor (payload shape: field id, Meta, overwrite semantics) — completing the existing AddLink/AddComment pattern instead of the current 'stored as-is' generic field patch. This closes, rather than expands, the deliberately-small field-type set.
- Committed JSONL export as the canonical workspace representation, with SQLite as a derived/rebuildable artifact — a source-of-truth inversion, not a schema change. It creates a two-writer problem (live DB vs. git snapshot) whose honest resolution is the deferred D13 git-backed mirror / version-gated PATCH-import; dogfooding should be scoped one-way and treated as the evidence-gathering phase for D13.
- Commit-vs-dispatch decoupling as an explicit contract: once a tailer exists, 'crash after commit, before dispatch' flips from documented tradeoff (EVENTS.md §8.5) to regression class. That guarantee change should be written into EVENTS.md as a versioned contract, not implied by implementation.

**Sequencing:** Phase 0 (days): clear the cheap load-bearing debt — ClaimAtomic cluster (DEBT-01/02/30/17), DEBT-29 lifecycle pattern, ledger/doc refresh (DEBT-42-46, DEBT-57 correction), repo hygiene (archive root artifacts, resolve sprint-plan deletion). This unblocks everything and nothing depends on anything else. Phase 1 (parallel, independent tracks): (a) dogfood-own-backlog (S) with the doc-id CI check — it generates the real usage signal that determines whether the XL outbox is justified, and gives every later feature a live test bed; (b) wire-attachments (M) — it touches core/httpapi/mcp/cli but not the events-delivery path, so it can proceed independently once the shared Manager-construction helper and the byte-transport design note exist; route-level adversarial path-traversal tests are its gate. Phase 2 (gated on design): write the Subscriber RFC (unified cursor/liveness/dead-letter model, hooks-migration decision, static-vs-runtime extension fork, auth/SSRF note for /v1/subscribers). Do not start the outbox before this RFC — its two biggest risks (duplicate liveness models, hooks-migration ambiguity) are design failures, not coding failures. Phase 3: build outbox + webhooks as one sequenced effort with webhooks as the outbox's first real consumer — the webhook card is what makes Step 4 necessary rather than speculative infrastructure, and building the outbox alone would repeat the over-investment pattern its own risk list warns about. Net: dogfooding unblocks the outbox go/no-go decision; the ClaimAtomic fix unblocks all durable delivery; the Subscriber RFC unblocks both Phase-3 builds; attachments is unblocked immediately and blocks nothing.

## Appendix B — candidates considered
- **Wire attachments end-to-end (upload + serve + CLI verb + MCP tool + emit artifact_added)** — This is the highest-leverage, lowest-risk net-new capability within reach because the hard, dangerous part is already built and tested: the content-addressed store (internal/artifacts) has SHA-256 addressing, MIME sniffing, and path-confinement (DEBT-57) done. What remains is thin, well-understood surface plumbing — an upload endpoint, a serve route, a `cards` verb, an MCP tool, and firing the already-defined-but-unused artifact_added event constant. Doing it also retires the single largest built-but-dark capability in the repo (ROADMAP §4 / card dafd0873), converting sunk cost into shipped value. Keeping all four surfaces (HTTP/CLI/MCP/event) in lockstep is a one-session job precisely because the contract is already singular.
- **Events Step 4 — durable outbox/tailer delivery substrate** — This is the north-star move. Step 3 condition events (status_timeout, card_idle, card_blocked/unblocked, the tickless deadline scheduler, persist:true escalation) are fully built and firing, but delivery is still best-effort in-process SSE with a real commit-then-crash-before-dispatch gap. Until a durable cursor + dead-letter outbox exists, the entire large events investment carries engine-shaped complexity with none of the external-consumption payoff that justifies it philosophically. Landing the outbox is the explicitly-designed next milestone (marked [future] by intent, not neglect) and is the structural prerequisite for every downstream consumer.
- **Webhook delivery kind + /v1/subscribers status surface** — Built directly on the Step-4 outbox, a webhook delivery kind is what finally makes the events engine externally composable in a way that satisfies the 'resist anything expressible as `cards events stream` plus a script' test — because now the core delivers conditions outward and the reaction genuinely lives in an independent process (principle 6). A /v1/subscribers surface makes the fan-out observable and debuggable. Sequenced correctly this is the second half of the north star and the concrete proof of the composition thesis; it should follow, not precede, the durable substrate so deliveries are backed by the outbox's retry/dead-letter guarantees.
- **Dogfood this repo's own backlog on a live cards board** — The project's thesis is that agents and humans coordinate on typed cards, yet its own planning runs entirely on markdown (ROADMAP/NOTES/action-plan) with a 0-byte root work-cards.db and both live instances serving picraft — so the many card IDs cross-referenced in docs point at a board absent from this checkout. This gap is the likely root cause of the recent cross-facet misread (a facet read the historical debt ledger as current state; a live board would have made status unambiguous). Standing up a real board seeded from ROADMAP is a modest, self-contained step that exercises the full HTTP/CLI/MCP/event stack against the maintainers' own workflow.

## Appendix C — review feedback incorporated (by the Fable revise)

**design-ux**
- [blocker] Phase 2 does not close the human evidence loop. Its stated goal is 'humans, scripts, and agents can post an artifact (log, diff, screenshot) onto a card,' but every step targets HTTP/CLI/MCP only. The product has a real human surface (internal/httpapi/templates/card_detail.html, /ui/cards/{id}), and that template renders NO comments, links, or artifacts today. So a human uploads a screenshot and cannot see it anywhere in the browser — the loop closes for agents/API consumers and stays open for the human. A screenshot you can only GET as raw bytes over HTTP is not shipped user value for a human. → Add a Phase 2 step that renders attachments on card_detail (inline thumbnail for images, download link + MIME/size for others), and add artifact_added to the board's SSE type filter (ui.go boardSSETypes()) so an attached artifact appears on the live board without reload. Make the Phase 2 demo include 'attach a screenshot and see it on the card in the browser,' not just byte-identical GET.
- [major] The plan is blind to the existing /ui web surface across all five phases — the single human-facing surface in a product whose thesis is 'agents AND humans coordinate on typed cards' receives zero attention, while every phase and every demo serves API/CLI/MCP/agent consumers. This is the core design/UX gap: 'visible user value' is asserted but the human's visual surface is never touched or demoed. → Make the web UI a first-class consumer in each phase's exit criteria and demo where it applies (attachments rendered in Phase 2, board dogfooded in Phase 3, live-board integrity in Phase 4, subscriber health surfaced in Phase 5). At minimum, name /ui explicitly so it stops being an invisible fourth surface that silently drifts.
- [major] Phase 3 dogfoods the 'live board' but excludes the actual visual board. There is a real board at /ui/boards/{id}; the phase runs the daily loop only through CLI/MCP/JSONL and captures friction only from those surfaces. That deliberately skips exactly where human-coordination UX friction lives — the web board and card views — for a phase whose whole point is validating the human+agent coordination thesis. The 'live-drive in front of the room' demo is also far weaker as a terminal session than on the real board. → Run the dogfooding loop through /ui as well as CLI/MCP; have the friction log cover the web board/card-detail experience; and drive the demo on the actual /ui board (with the Phase-2 attachment visibly appearing) rather than a terminal-only walkthrough.
- [major] Phase 4's async tailer changes SSE latency/ordering, and the live web board depends on SSE directly — board.html opens an EventSource on /v1/events/stream and re-renders on mutation events. The regression plan names only temporal_test.go and SSE 'timing' tests; it never mentions the human live-board re-render behavior, which can visibly regress (missed updates, out-of-order badges, stale columns) under the new async path. → Add an explicit Phase 4 acceptance criterion that the live /ui board still re-renders correctly and in order under the async tailer (a browser/integration check on EventSource-driven updates), alongside the temporal/SSE unit tests.
- [minor] Phase 1's demo proves correctness, not value. 'Suite green + fault-injection test + git status clean + a one-line ledger diff' is an engineer's demo; it demonstrates nothing a user or operator would recognize as value, in a phase that (with Phase 4) delivers no visible user value at all. The lens question 'demos that prove value' fails here. → Reframe the Phase 1 demo around the averted user-facing consequence: show an operation that previously would have silently committed a card while losing its event, now visibly rejected — i.e., 'here is the data-integrity failure a user would never have known about, and here it is caught.' Make the stakes legible, not just the test bar green.
- [minor] Phase 5 exposes subscriber/delivery/dead-letter health as JSON only (GET /v1/subscribers). For an operator, retry counts, last error, and dead-letter state buried in a raw JSON endpoint is poor UX — especially when a visual operational surface already exists (/ui/breaches). The one operationally-critical, human-monitored state in the sprint has no human view. → Either add a minimal /ui view for subscriber/delivery/dead-letter health (mirroring the breaches page), or explicitly record 'JSON-only, no operator UI' as a known, accepted UX gap with a follow-up — don't leave it implicit.

**arch-data**
- [blocker] Domain-concept collision on 'Subscriber'. `core.Subscriber` already exists (bus.go:50) as an ephemeral in-process bus listener: {ID int64, Filter, Ch chan}. Phase 4/5 introduce a *durable* 'Subscriber' entity (SUBSCRIBERS.md RFC, /v1/subscribers, subscriptions table, cursor + dead-letter). These are different species — the RFC's premise that SSE Last-Event-ID is 'a client-held cursor of the same species' does not hold for the existing struct, which has no cursor and no durability. Shipping both under one name guarantees ongoing confusion in code, tables, and the REST surface. → In the Phase-4 RFC, explicitly reconcile the name before any code: either rename the durable entity (e.g. Consumer/Delivery/Endpoint) or rename the ephemeral bus struct (e.g. BusListener). Define the mapping from the existing ephemeral `core.Subscriber` to the durable model rather than asserting they are the same species.
- [blocker] Ephemeral conditions are structurally undeliverable to durable/webhook consumers — a hidden coupling the plan never states. The tailer reads the events *table*; but condition events default to the Signal path (events.go:84 — dispatch only, no Append), so status_timeout, card_idle, wip_*, lane_*, card_blocked never enter the log. Phase 4's demo (replay a card_idle from a cursor) and Phase 5's demo (status_timeout webhook surviving restart) are impossible unless those types are escalated to persist:true. Worse, PersistConditions is documented as startup-only ('Configure once at startup, before emission', events.go:92), yet a runtime-mutable /v1/subscribers subscribing to an ephemeral type would have to mutate that global config at runtime and re-arm the MonitorScheduler (which keys arming off IsPersisted, monitor.go:170). → Make the persist coupling a first-class RFC decision: registering a durable/webhook subscriber for an ephemeral condition type must either auto-escalate that type to durable (and the plan must resolve how that interacts with the startup-only PersistConditions invariant and MonitorScheduler arming) or be rejected at registration. Document per-type deliverability so a Subscriber.filter over event types is not silently a no-op for half the catalog.
- [blocker] mark-then-append atomicity gap defeats the outbox's core promise on the exact path it demos. In fireOne (monitor.go:340-349) MarkConditionFired commits in one transaction, then emitter.Condition→Emit→log.Append runs in a *separate* transaction. A crash between them permanently loses the event (never appended) AND marks it fired (never re-fires). The durable outbox cannot recover this — the gap is upstream of the log, so no cursor replay helps. Phase 4's 'stops being drop-on-crash / kill -9 mid-dispatch and replay card_idle' claim is false for escalated conditions until mark and append are made atomic. → Add to Phase 4 (or Phase 1 hardening): for persist:true conditions, write the fired-marker and the durable event append in one transaction (or append-then-mark with idempotent replay), so the condition_marks row and the log row commit together. Call this out as a correctness prerequisite for the outbox, not a Phase-4 incidental.
- [major] Phase 3's 'SQLite is a derived/rebuildable artifact' contradicts Phase 1 and orphans all delivery state. The append-only event journal (Phase 1: 'ground truth every downstream consumer trusts'), condition_marks, and the Phase 4/5 cursor / dead-letter / subscriptions tables live *only* in SQLite and are NOT derivable from a card-state JSONL export. Rebuilding the DB from the committed JSONL — the whole point of Phase 3 — wipes event history and all durable-delivery bookkeeping. 'SQLite is derived' is only coherent for current card state, not for history or delivery state. → Scope the Phase-3 export explicitly: JSONL is authoritative for card definitions + current card state only; the event journal, condition_marks, cursors, dead-letters, and subscriptions remain SQLite-owned and are never reconstructed from the export. State that rebuild-from-JSONL is a card-state operation that does not reconstitute history/delivery, so Phase 4/5 do not build durable state on a substrate Phase 3 declares disposable.
- [major] Tailer republish vs. existing synchronous dispatch: double-delivery or a broken observer contract. Emit already publishes to the bus and observers synchronously at commit (events.go:171-184); the hook supervisor is a live bus subscriber with an empty filter (hooks.go:65). A Phase-4 tailer that also 'republishes to bus/observers' from the log would deliver every persisted event twice to bus subscribers (SSE, hooks). Avoiding that requires re-routing SSE/observers to pull-from-cursor, which breaks the documented 'observers notified synchronously for every dispatched event' contract and changes hook-firing timing. The relationship between synchronous Emit-dispatch and async tailer-dispatch is left undefined. → Pin the dispatch topology in the RFC before Phase 4 coding: decide whether the tailer feeds only *new* durable consumers (bus/observers stay on the synchronous path, no republish) or replaces synchronous dispatch wholesale (and then re-specify the EventObserver/hook synchrony contract). Do not leave 'republishes to bus/observers' as an implementation detail.
- [major] Phase 1 Step 1's headline fix appears already satisfied at HEAD, which undercuts the foundation-first sequencing rationale. Current ClaimAtomic (sqlite.go ~643) returns and rolls back on both event-insert (execEventInsert) and FTS (upsertFTS) failure, and builds events via the shared core.OwnerChanged/StatusChanged constructors (Version:1 via CardEvent), not hand-rolled Version:0 literals. The DEBT-01/02/30/17 description in the plan does not match the code. → Re-verify DEBT-01/02/30/17 against HEAD before scheduling the work. If already fixed, either drop the step or re-anchor Phase 1's 'harden before layering' argument on a real remaining gap (e.g. the mark-then-append atomicity above, or DEBT-06 SSE replay), so the sequencing justification stands on something true.

**reliability**
- [blocker] Phase 3 demotes SQLite to a "derived/rebuildable" artifact reconstructed from the committed JSONL export, but (a) the export path serializes cards, not the events table (see exportJSONL/GetCard, DEBT-10), and (b) Phase 4 puts the durable outbox cursors and dead-letter records in that same SQLite DB. So a rebuild from JSONL silently destroys the event log and all delivery/dead-letter state. This directly contradicts Phase 1's own premise that the events table is the durable ground truth every consumer trusts, and Phase 4's at-least-once-across-restart guarantee. → Reconcile the two framings explicitly before either phase starts. Either scope Phase 3's "canonical JSONL" to definitions/card-state only while declaring the events table + cursor/dead-letter tables as authoritative, non-rebuildable durable state (not derived), or make the export/import round-trip events too. State in an exit criterion that rebuilding the DB must never drop event history or outbox delivery state.
- [major] Phase 1 Step 1's premise is stale. The current ClaimAtomic (internal/sqlite/sqlite.go ~700-720) already uses core.OwnerChanged/core.StatusChanged (no hand-rolled Version:0 events) and already returns/rolls back on execEventInsert and upsertFTS failure, with code comments to that effect. DEBT-01/30's cited lines (445-456) no longer match the code. The sprint sequences everything "foundation-first" behind a foundation that is largely already built, and would send a dev to re-fix fixed code. → Re-scope Phase 1 Step 1 against live code, not the ledger. Confirm what actually remains (e.g. DEBT-02 scanCard error masking, the non-ClaimAtomic swallowed-error sites in service.go AddLink/AddComment DEBT-03) and target those; drop the already-done ClaimAtomic version/rollback work or reduce it to a regression test that pins the existing behavior.
- [major] The plan is built on a debt ledger it simultaneously admits is stale. Phase 1 Step 3 corrects exactly one stale entry (DEBT-57, artifacts.Manager) but the same staleness invalidates Phase 1 Step 1 (ClaimAtomic) and part of Step 2 (DEBT-29 supervisor lifecycle is already partly handled: serve.go ties the supervisor ctx to ListenAndServe via defer cancel()). Reconciliation is sequenced third, after the fix steps whose scoping depends on it. → Move ledger/inventory/EVENTS.md reconciliation to the FIRST step of Phase 1 and re-derive every downstream step's scope from verified live code. Treat any ledger line:number reference as unverified until checked against the current file.
- [major] The go/no-go decision for the XL outbox exists only as risk-section prose. Phase 4 hard-dependsOn Phase 3 and the sprint theme commits to building the outbox regardless, so the "revisit before committing Phase 4" gate has no teeth: no defined signal threshold, no branch in the phase structure, no exit criterion that can read "no-go". → Make the go/no-go an explicit gate artifact from Phase 3 with a concrete, pre-registered criterion (e.g. measured dropped-event rate / classes of events lost across an induced crash that the team actually needs). Define what the sprint does if the answer is no-go (which later phases are cut).
- [major] Operational scope risk: five phases including an XL (outbox) and an L (webhooks) plus real core work in Phase 2 (Service.AddArtifact interacting with optimistic-concurrency versioning) in a single sprint. Durable-delivery code rushed under end-of-sprint pressure is exactly where crash-safety and ordering bugs hide, and the plan itself names subscriber-model divergence as its top design risk. → Split the durable-delivery work off. Land Phases 1-3 (+ the SUBSCRIBERS RFC) as one sprint; make the outbox+webhooks (Phases 4-5) a separate sprint gated on the Phase 3 signal and the agreed RFC. This also lets the RFC be reviewed cold rather than under coding pressure.
- [major] /v1/subscribers ships a runtime-registrable, arbitrary-outbound-URL entity (the first runtime-mutable, extension-like object in an otherwise file-declared extension model) with SSRF/exfiltration mitigation deferred to "a documented trusted-network assumption" rather than an enforced control. A documented assumption is not a failure-mode control. → Require an actually-enforced default-deny outbound allowlist (host/CIDR) as a shipping exit criterion for Phase 5, not just an RFC note. If that can't land, do not expose runtime registration this sprint; keep webhook subscribers file-declared like every other extension until the allowlist exists.
