// Package main is the entry point for the cards binary. It wires CLI
// subcommands to the shared service layer in internal/core.
package main

func main() {
	// TODO: cobra root command dispatching to serve / mcp / rpc / list / get /
	// create / patch / claim / take-next / append / link / comment / events /
	// workspace / boards / views / users / extensions / export / import.
	//
	// See docs/ARCHITECTURE.md and docs/SPEC.md (v0.4).
}
