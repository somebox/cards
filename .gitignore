# Go
/bin/
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
coverage.html

# Build output
/cards
/cmd/cards/cards

# Workspace runtime state (local dev)
/.work-cards/
*.db
*.db-*
/artifacts/
/mirror/
/.cards/logs/
/.cards/sessions/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# runtime demo database — never commit, never wipe
examples/demo-workspace/work-cards.db
examples/demo-workspace/work-cards.db-*
examples/demo-workspace/export.jsonl

# artifact bytes stored per workspace (content-addressed; never commit)
examples/demo-workspace/artifacts/

# Claude Code: share workflows, keep local settings out
.claude/settings.local.json

# pi agent working artifacts (dev-server tmp/run, audit scratch)
/.pi/

# pi subagent review runs (transcripts, scratch outputs — not portable state)
/.pi-subagents/

# Cursor IDE — share slash commands; keep local IDE state out
.cursor/*
!.cursor/commands/
!.cursor/commands/**

# MkDocs build output
/site/

# pi-pipeline run artifacts (local; not committed)
.pi/pipeline/
